/** @jsx React.DOM */

var React = require('react');

var App = module.exports = React.createClass({

  render: function() {
    return (
      <div>
        <h2>Logout</h2>
      </div>
    );
  }

});

